class Tap extends AutoClick{
    constructor(pRate,pAmount,pName, pPrice) {
       super();
       this.SetClickRate(pRate);
       this.SetClickAmount(pAmount);
       // this.mName = pName;
       // this.mPrice = pPrice;
    }
}